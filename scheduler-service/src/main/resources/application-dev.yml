server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/scheduler_dev?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
  redis:
    host: localhost
    port: 6379

xxl:
  job:
    admin:
      addresses: http://localhost:8080/xxl-job-admin
    accessToken: dev_token

rocketmq:
  name-server: localhost:9876
  producer:
    group: scheduler-producer-dev