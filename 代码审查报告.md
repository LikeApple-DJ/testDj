# 📋 代码审查报告

> 项目：testDj（后端 Spring Boot）+ testDJnew（前端 Vue3）
> 审查时间：2025年8月

---

## 一、项目结构总览

```
testDj-main/                          # 后端 Java Spring Boot
├── src/main/java/com/example/demo/
│   ├── controller/
│   │   ├── HelloController.java       # HelloWorld API
│   │   ├── HashController.java        # 哈希算法 API
│   │   ├── SortController.java        # 冒泡排序 API
│   │   ├── ExportController.java      # CSV 导出 API
│   │   ├── StatsController.java       # 统计看板 API
│   │   └── AuthController.java        # JWT 登录 API
│   ├── service/
│   │   ├── AlgorithmService.java      # 算法实现
│   │   ├── ExportService.java         # 导出逻辑
│   │   ├── StatsService.java          # 统计逻辑
│   │   ├── PersonService.java         # 模拟人员数据
│   │   └── AuthService.java           # 认证逻辑
│   ├── interceptor/
│   │   └── CallTrackingInterceptor.java  # 埋点拦截器
│   ├── config/
│   │   ├── AuthInterceptor.java       # JWT 鉴权拦截器
│   │   └── WebConfig.java             # Web 配置 + CORS
│   ├── model/
│   │   ├── CallRecord.java            # 调用记录实体
│   │   └── UserInfo.java              # 用户信息实体
│   └── repository/
│       ├── CallRecordRepository.java  # 调用记录 DAO
│       └── UserInfoRepository.java    # 用户信息 DAO
├── src/main/resources/
│   ├── application.yml                # 配置（H2 + JWT）
│   └── data.sql                       # 初始化数据
└── pom.xml

testDJnew-main/                       # 前端 Vue 3
├── src/
│   ├── App.vue                        # 主应用（登录态 + 路由）
│   ├── main.js                        # 入口
│   ├── api/index.js                   # API 封装（axios）
│   ├── router/index.js                # 路由（含 auth 守卫）
│   ├── store/auth.js                  # Pinia 认证状态
│   ├── utils/export.js                # 下载工具
│   ├── views/
│   │   ├── Login.vue                  # 登录页
│   │   └── Dashboard.vue              # 主面板（3个Tab）
│   └── components/
│       ├── HelloTab.vue               # HelloWorld Tab
│       ├── HashTab.vue                # 哈希算法 Tab
│       ├── SortTab.vue                # 冒泡排序 Tab
│       ├── ExportButton.vue           # 导出按钮
│       ├── StatsChart.vue             # 统计看板容器
│       ├── BarChart.vue               # 柱状图
│       ├── LineChart.vue              # 折线图
│       └── PieChart.vue               # 饼图
├── package.json
├── vite.config.js                     # 代理配置
└── index.html
```

---

## 二、后端 API 接口清单

| 接口 | 方法 | 说明 | 鉴权 |
|------|------|------|------|
| `/api/hello` | GET | 返回 "Hello World!" | ✅ |
| `/api/hash` | POST | 哈希算法（SHA-256/MD5/SHA-512/SHA-1） | ✅ |
| `/api/sort/bubble` | POST | 冒泡排序，返回排序结果和耗时 | ✅ |
| `/api/export?tab=` | GET | CSV 导出（按 tab 过滤） | ✅ |
| `/api/stats/calls?dimension=&start=&end=` | GET | 按维度统计调用数据 | ✅ |
| `/api/stats/callers` | GET | 获取调用人员列表 | ✅ |
| `/api/auth/login` | POST | JWT 登录 | ❌ |

---

## 三、审查结果

### ✅ 通过项

| 检查项 | 状态 | 说明 |
|-------|------|------|
| **HelloWorld** `/api/hello` GET | ✅ | 调用 `AlgorithmService.hello()` 返回 "Hello World!" |
| **哈希算法** `/api/hash` POST | ✅ | 支持 SHA-256/MD5/SHA-512/SHA-1 四种算法 |
| **冒泡排序** `/api/sort/bubble` POST | ✅ | 实现完整冒泡排序，返回排序后数组和耗时 |
| **导出接口** `/api/export?tab=` GET | ✅ | CSV 导出，按 tab 参数过滤数据 |
| **统计接口** `/api/stats/calls` GET | ✅ | 支持 type/level/dept/time 四种维度 |
| **埋点拦截器** | ✅ | 记录调用人/类型/层级/部门/耗时到数据库 |
| **JWT 认证** | ✅ | 登录获取 token，`Authorization: Bearer <token>` 鉴权 |
| **模拟人员数据** | ✅ | 10 个用户，3 种类型/4 个层级/3 个部门 |
| **H2 初始化数据** | ✅ | 20 条模拟调用记录用于图表展示 |
| **三个 Tab** | ✅ | HelloWorld / 哈希算法 / 冒泡排序 |
| **导出按钮** | ✅ | 按当前 Tab 导出 CSV |
| **折线图** | ✅ | 调用趋势（按天） |
| **饼图** | ✅ | 人员分布（类型/部门可切换） |
| **柱状图** | ✅ | 人员层级分布 |
| **登录页** | ✅ | JWT 认证 |
| **路由守卫** | ✅ | 未登录自动跳转登录页 |
| **API 代理** | ✅ | vite 配置代理到 localhost:8080 |

### ⚠️ 建议项

1. **后端** - `CallTrackingInterceptor` 中 H2 `FORMATDATETIME` 函数非标准 SQL，但 H2 兼容，demo 场景无问题
2. **前端** - `HelloTab` 等组件中 `result.value = res.data` 获取后端返回数据，与 axios 拦截器逻辑一致，无问题
3. **整体架构** - 代码结构清晰，Controller/Service/Repository 关注点分离

---

## 四、启动方式

### 后端启动
```bash
cd testDj-main
mvn spring-boot:run
# 启动在 http://localhost:8080
```

### 前端启动
```bash
cd testDJnew-main
npm install
npm run dev
# 启动在 http://localhost:5173
```

### 默认账号
- 用户名：`admin` / `user1` ~ `user9`
- 密码：`123456`